import React from 'react';
import { Button } from 'react-bootstrap';

const CounterThree = () => {
    return (
        <div>
              <Button className='mx-2' variant="primary">16+</Button>
        </div>
    );
};

export default CounterThree;